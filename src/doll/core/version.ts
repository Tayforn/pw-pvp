// Версія ядра ляльки. Піднімати при БУДЬ-ЯКІЙ зміні формул чи констант у src/doll/core
// (guard.test.ts звіряє sha256 файлів ядра зі списком у SYNC.md і з історією нижче).
export const DOLL_ENGINE_VER = 1;

// Історія ядра: версія → sha256 блоку хешів файлів ядра (рядки «- файл.ts: sha256»
// у порядку імен через \n — той самий блок, що в SYNC.md). Лише ДОПИСУВАТИ: змінив
// ядро — новий запис з новою версією, старі не чіпати. Сторож вимагає, щоб поточне
// ядро мало хеш саме запису DOLL_ENGINE_VER, записи йшли 1..N без пропусків і не
// повторювались — тож «вставити надрукований блок і не підняти версію» не пройде:
// новий хеш під старою версією означав би переписати історію, а це видно в диффі.
export const DOLL_ENGINE_HASHES: Readonly<Record<number, string>> = {
  1: '722bd66a7e40323b978ced6d356042c5f17bb6a83a9fb4109a59f4b094bb28e3',
};

// Коміт pw-calc, з якого портовано ядро (golden-еталони згенеровано з нього).
export const CALC_SYNC_COMMIT = 'e0a79ed';
